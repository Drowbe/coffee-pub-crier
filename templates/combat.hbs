<span style="visibility: hidden">coffeepub-hide-header</span>
<div class="blacksmith-card {{theme}}" data-combat-id="{{combat.id}}" data-announcement-kind="{{kind}}">
	<div class="card-header">
		<i class="fa-solid {{iconStyle}}"></i> {{message}}
	</div>
</div>
